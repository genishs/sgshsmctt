# 쉘 스크립트는 컨테이너에서 실행되므로 항상 LF 유지
*.sh text eol=lf

# 배치 파일은 cmd.exe 가 CRLF 를 기대하므로 항상 CRLF 유지
*.bat text eol=crlf

# 일반 텍스트 파일은 플랫폼 기본값 사용
*.md text
*.yml text
*.yaml text
*.properties text
*.json text
